# frozen_string_literal: true

RSpec.describe RubyIso20022 do
  it "has a version number" do
    expect(RubyIso20022::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
